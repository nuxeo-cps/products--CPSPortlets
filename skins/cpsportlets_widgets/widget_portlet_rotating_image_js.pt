function showimage(layer_ref) {
  if (document.getElementById &&!document.all) {
    var div = document.getElementById(layer_ref);
    div.className = 'visible';
  }
}

function get_random(n) {
  var ranNum= Math.round(Math.random()*n);
  return ranNum;
}
